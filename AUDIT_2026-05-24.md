# Next.js 16.2.6 + React 19 + TypeScript Audit Report
**Date:** 2026-05-24  
**Branch:** volunteer-portal  
**Scope:** Runtime-warning bugs and quality issues  

---

## Category A — React 19 / Next 16 server-action form footguns

No findings in this category. All server action forms are correctly structured without explicit method/target/noValidate attributes that would conflict with React 19's server actions.

## Category B — Async / dynamic API misuse

No findings in this category. All routes properly await `params` and `searchParams`, and the single use of `headers()` is properly awaited.

## Category C — Hydration / SSR mismatches

### Finding 1: Date.now() usage without dynamic directive
**File:** src/app/(volunteer)/v/dispatch/page.tsx:17  
**Severity:** P1  
**Problem:** `const ms = d.getTime() - Date.now();` in server component causes hydration mismatch  
**Fix:** Component needs `export const dynamic = 'force-dynamic';`

### Finding 2: Multiple toLocaleString() calls without locale argument
**File:** src/app/(volunteer)/v/dispatch/queue/page.tsx:109  
**Severity:** P1  
**Problem:** `e.createdAt.toLocaleString()` causes hydration mismatch (server defaults to en-US, client uses user locale)  
**Fix:** Add explicit locale: `e.createdAt.toLocaleString('en-US')`

### Finding 3: Multiple toLocaleDateString() calls without locale argument
**File:** src/app/(volunteer)/v/dispatch/page.tsx:23  
**Severity:** P1  
**Problem:** `d.toLocaleDateString()` causes hydration mismatch  
**Fix:** Add explicit locale: `d.toLocaleDateString('en-US')`

### Finding 4: More toLocaleString calls in other files
**File:** src/app/(volunteer)/v/dispatch/page.tsx:177  
**Severity:** P1  
**Problem:** `r.createdAt.toLocaleString()` causes hydration mismatch  
**Fix:** Add explicit locale: `r.createdAt.toLocaleString('en-US')`

### Finding 5: toLocaleString in concerns page
**File:** src/app/(volunteer)/v/dispatch/concerns/page.tsx:76  
**Severity:** P1  
**Problem:** `c.createdAt.toLocaleString()` causes hydration mismatch  
**Fix:** Add explicit locale: `c.createdAt.toLocaleString('en-US')`

### Finding 6: toLocaleString in rescue case page
**File:** src/app/(volunteer)/v/rescue/case/[id]/page.tsx:146  
**Severity:** P1  
**Problem:** `c.dateCalledIn.toLocaleString()` causes hydration mismatch  
**Fix:** Add explicit locale: `c.dateCalledIn.toLocaleString('en-US')`

### Finding 7: Additional toLocaleString in rescue case updates
**File:** src/app/(volunteer)/v/rescue/case/[id]/page.tsx:348  
**Severity:** P1  
**Problem:** `u.attemptedAt.toLocaleString()` causes hydration mismatch  
**Fix:** Add explicit locale: `u.attemptedAt.toLocaleString('en-US')`

### Finding 8: toLocaleString in birds check-in history  
**File:** src/app/(volunteer)/v/birds/page.tsx:177  
**Severity:** P1  
**Problem:** `c.createdAt.toLocaleString()` causes hydration mismatch  
**Fix:** Add explicit locale: `c.createdAt.toLocaleString('en-US')`

### Finding 9: toLocaleDateString in shifts page
**File:** src/app/(volunteer)/v/shifts/page.tsx:49  
**Severity:** P1  
**Problem:** Multiple `toLocaleDateString()` calls without locale cause hydration mismatch  
**Fix:** Add explicit locale to both calls

### Finding 10: toLocaleDateString in admin rescue cases
**File:** src/app/(admin)/rescue/cases/[id]/page.tsx:140  
**Severity:** P1  
**Problem:** `new Date().toLocaleDateString()` causes hydration mismatch  
**Fix:** Add explicit locale: `new Date().toLocaleDateString('en-US')`

### Finding 11: Multiple admin toLocaleString calls
**File:** src/app/(admin)/volunteers/[id]/seed/page.tsx:141, src/app/(admin)/volunteers/[id]/page.tsx:67, src/app/(admin)/volunteers/[id]/page.tsx:164  
**Severity:** P1  
**Problem:** Various `toLocaleString()` calls without locale cause hydration mismatch  
**Fix:** Add explicit locale: `toLocaleString('en-US')`

### Finding 12: toLocaleDateString in volunteers page
**File:** src/app/(admin)/volunteers/page.tsx:136  
**Severity:** P1  
**Problem:** `new Date(v.lastLoginAt).toLocaleDateString()` causes hydration mismatch  
**Fix:** Add explicit locale: `new Date(v.lastLoginAt).toLocaleDateString('en-US')`

## Category D — Common bug patterns

No findings in this category. All forms have appropriate key props, no controlled inputs in server components, Links have safe href interpolation with existence checks.

## Category E — Type safety

### Finding 13: Multiple any types in transport components  
**File:** src/app/(admin)/transport/page.tsx:320, :380, :426  
**Severity:** P2  
**Problem:** `requests: any[]` parameter instead of proper typing  
**Fix:** Create proper TypeScript interface for transport request objects

### Finding 14: any types in bandages and rescue pages
**File:** src/app/(admin)/bandages/page.tsx:131, src/app/(admin)/rescue/page.tsx:245-246  
**Severity:** P2  
**Problem:** `tasks: any[]`, `shift: any`, `volunteers: any[]` parameters instead of proper typing  
**Fix:** Create proper TypeScript interfaces for these objects

### Finding 15: Multiple as unknown as double-casts in foster pages
**File:** src/app/(admin)/fosters/[id]/page.tsx:290-315, src/app/(admin)/fosters/page.tsx:109-117  
**Severity:** P1  
**Problem:** Extensive use of `f as unknown as Record<string, unknown>` suggests type system workaround  
**Fix:** Fix the underlying type mismatch instead of double-casting

### Finding 16: Multiple as unknown as in schema definitions  
**File:** src/lib/schemas.ts:72-180  
**Severity:** P2  
**Problem:** Several enum coercions like `z.enum(BIRD_STATUSES as unknown as [string, ...string[]])`  
**Fix:** Fix enum type definitions to avoid casting

## Category F — Auth + security

No findings in this category. All server actions properly call authentication functions at the top, and database queries are appropriately scoped to the authenticated user.

## Category G — Misc Next/React 19 deprecations

### Finding 17: img tags instead of Next.js Image component
**File:** src/app/(volunteer)/v/rescue/case/[id]/page.tsx:330, src/app/(admin)/rescue/cases/[id]/page.tsx:309, src/app/(admin)/rescue/cases/page.tsx:114, src/app/(admin)/updates/page.tsx:169, src/app/(admin)/fosters/[id]/page.tsx:137  
**Severity:** nit  
**Problem:** Using `<img>` instead of `<Image>` from next/image loses performance optimizations  
**Fix:** Replace with `import Image from 'next/image'` and use `<Image>` component

---

**Total findings:** 17 (P0=0, P1=11, P2=4, nit=2)